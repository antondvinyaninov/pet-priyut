# План оптимизации загружаемых файлов

## Цель
Автоматическое сжатие и оптимизация файлов при загрузке для экономии места и ускорения работы системы.

## 1. Оптимизация изображений

### Фотографии животных
- **Формат:** Конвертация в WebP (лучшее сжатие при сохранении качества)
- **Размеры:**
  - Оригинал: сохранять как есть (для печати карточек)
  - Превью для списка: 300x300px
  - Для карточки животного: 800x800px
- **Качество:** 85% (оптимальный баланс)
- **Библиотека:** Intervention Image (уже есть в Laravel)

### Медицинские справки и документы (сканы)
- **Формат:** JPEG с оптимизацией
- **Качество:** 90% (важна читаемость)
- **Максимальный размер:** 1920px по длинной стороне

## 2. Оптимизация видео

### Видео выездов
- **Формат:** MP4 (H.264)
- **Разрешение:** 
  - FullHD (1920x1080) → оставить как есть
  - Выше FullHD → сжать до 1920x1080
- **Битрейт:** 2-4 Mbps (достаточно для FullHD)
- **Библиотека:** FFmpeg
- **Обработка:** Асинхронная через очередь (чтобы не блокировать загрузку)

## 3. Техническая реализация

### Этап 1: Изображения (приоритет)
```php
// app/Services/ImageOptimizationService.php
- Автоматическое создание превью
- Конвертация в WebP
- Сохранение оригинала для печати
- Удаление EXIF данных (кроме ориентации)
```

### Этап 2: Видео (опционально)
```php
// app/Jobs/OptimizeVideoJob.php
- Асинхронная обработка через очередь
- Сжатие до оптимального битрейта
- Генерация превью (первый кадр)
- Уведомление пользователя о завершении
```

### Этап 3: Документы
```php
// app/Services/DocumentOptimizationService.php
- Оптимизация PDF (если загружаются)
- Сжатие JPEG сканов
```

## 4. Настройки хранилища

### Структура папок
```
storage/app/public/
├── animals/
│   ├── originals/      # Оригиналы (для печати)
│   ├── large/          # 800x800 (для карточек)
│   └── thumbnails/     # 300x300 (для списков)
├── videos/
│   ├── originals/      # Оригинальные видео
│   ├── optimized/      # Сжатые видео
│   └── thumbnails/     # Превью видео
└── documents/
    ├── originals/      # Оригинальные документы
    └── optimized/      # Оптимизированные
```

## 5. Экономия места

### Примерные расчеты
**Без оптимизации:**
- Фото животного (оригинал): 5MB
- 263 животных × 2 фото = 2.6GB

**С оптимизацией:**
- Оригинал: 5MB (сохраняем)
- WebP large: 200KB
- WebP thumbnail: 50KB
- Итого на животное: ~5.25MB
- 263 животных × 2 фото = 2.76GB

**Но для отображения используем WebP:**
- Загрузка страницы: 50KB вместо 5MB (в 100 раз быстрее!)

**Видео:**
- Оригинал FullHD 5 мин: 200MB
- Оптимизированное: 50-80MB (экономия 60-75%)

## 6. Приоритеты внедрения

1. ✅ **Высокий:** Оптимизация фото животных (используются чаще всего)
2. ⚠️ **Средний:** Оптимизация видео (большие файлы, но реже загружаются)
3. 📋 **Низкий:** Оптимизация документов (если будут проблемы с местом)

## 7. Необходимые пакеты

```bash
# Для изображений (уже есть в Laravel)
composer require intervention/image

# Для видео (требует FFmpeg на сервере)
composer require pbmedia/laravel-ffmpeg
apt-get install ffmpeg  # в Dockerfile
```

## 8. Миграция существующих файлов

После внедрения оптимизации можно запустить команду:
```bash
php artisan optimize:existing-images
php artisan optimize:existing-videos
```

## Примечания

- Оригиналы всегда сохраняются (для печати карточек)
- Оптимизация происходит автоматически при загрузке
- Для видео - асинхронная обработка (не блокирует пользователя)
- WebP поддерживается всеми современными браузерами
